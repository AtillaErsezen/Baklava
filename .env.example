# Copy to .env and fill in. .env is gitignored: never commit it and never paste keys into chat.
NEBIUS_API_KEY=
TAVILY_API_KEY=
SUPABASE_URL=
# secret key: backend only, bypasses RLS
SUPABASE_KEY=
# publishable key: safe in the browser, read-only through RLS
SUPABASE_PUBLISHABLE_KEY=
# any phrase; the web app refuses runs without it
ACCESS_CODE=
AGENT_MODEL=Qwen/Qwen3-235B-A22B-Instruct-2507
# 1 only if the Modal workspace has GPU billing (TabICL)
ML_FACTORY_GPU=0
# optional: Modal token instead of `modal setup`
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
